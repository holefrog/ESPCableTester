# ESP32 Cable Tester - 代码改进方案

## 问题 P0: 校准参数无上界检查

### 现状问题

**文件**: `src/main.cpp`, 第 201~222 行

当前校准逻辑没有验证计算出的 `perM` 是否合理：

```cpp
for (int i = 0; i < 4; i++) {
    if (step2Cycles[i] <= tempBaseCycles[i]) {
        ok = false;
        break;
    }
    float diff = step2Cycles[i] - tempBaseCycles[i];
    perM[i] = (uint32_t)(diff / 1.9304f);  // ⚠️ 无范围检验！
}
```

### 风险场景

用户误用 6 英寸短线代替 76 英寸标准线进行校准，导致 `perM` 计算为极端值，后续所有测长都失效。

### 改进方案

将参数检验添加到校准 Step 2：

```cpp
// 在 main.cpp 顶部的常数定义区添加：
const uint32_t CALIB_CYCLES_PER_M_MIN = 350;  // CAT6 屏蔽线最低值
const uint32_t CALIB_CYCLES_PER_M_MAX = 750;  // CAT5e 标准线最高值
const uint32_t CALIB_CYCLES_PER_M_TYPICAL = 540;  // 典型值

// 然后在 STATE_CALIB_WAIT_76INCH 分支中修改：
} else if (appState == STATE_CALIB_WAIT_76INCH) {
    if (singleClick || doubleClick || longPress) {
      display.renderMessage("Sampling...");
      uint32_t step2Cycles[4];
      tester.runCalibrationSample(step2Cycles);

      uint32_t perM[4];
      bool ok = true;
      
      for (int i = 0; i < 4; i++) {
        if (step2Cycles[i] <= tempBaseCycles[i]) {
          ok = false;
          printf("ERROR: step2[%d] (%.0f) <= base[%d] (%.0f)\n", i, 
                 (float)step2Cycles[i], i, (float)tempBaseCycles[i]);
          break;
        }
        
        float diff = step2Cycles[i] - tempBaseCycles[i];
        perM[i] = (uint32_t)(diff / 1.9304f);
        
        // ✅ 新增：合理性检验
        if (perM[i] < CALIB_CYCLES_PER_M_MIN || 
            perM[i] > CALIB_CYCLES_PER_M_MAX) {
          ok = false;
          printf("ERROR: perM[%d] = %u is out of range [%u, %u]\n",
                 i, perM[i], CALIB_CYCLES_PER_M_MIN, CALIB_CYCLES_PER_M_MAX);
          break;
        }
      }

      if (ok) {
        appConfig.saveCalibration(tempBaseCycles, perM, tester);
        display.renderCalibDone();
        delay(1000);
        appState = STATE_NORMAL;
        isFirstRun = true;
      } else {
        // ✅ 改进错误提示
        const char* errMsg = "Invalid Calibration";
        const char* errDetail = "Check cable length";
        
        // 根据实际原因给出更具体的提示
        if (perM[0] < CALIB_CYCLES_PER_M_MIN || 
            perM[1] < CALIB_CYCLES_PER_M_MIN ||
            perM[2] < CALIB_CYCLES_PER_M_MIN ||
            perM[3] < CALIB_CYCLES_PER_M_MIN) {
          errDetail = "Cable too short!";
        } else if (perM[0] > CALIB_CYCLES_PER_M_MAX || 
                   perM[1] > CALIB_CYCLES_PER_M_MAX ||
                   perM[2] > CALIB_CYCLES_PER_M_MAX ||
                   perM[3] > CALIB_CYCLES_PER_M_MAX) {
          errDetail = "Cable too long!";
        }
        
        display.renderCalibError(errMsg, errDetail);
        delay(3000);
        appState = STATE_NORMAL;
        isFirstRun = true;
      }
    }
  }
```

### 验证步骤

1. 正常使用 76 英寸线校准 → 应成功保存
2. 使用 6 英寸线重复校准 → 应显示 "Cable too short!" 并拒绝保存
3. 使用超长线（>10 米）重复校准 → 应显示 "Cable too long!" 并拒绝保存

---

## 问题 P1: 防抖与UI刷新逻辑冗余

### 现状问题

**文件**: `src/main.cpp`, 第 59~99 行

存在两套独立的防抖逻辑，导致可能的屏幕闪烁和历史记录丢失：

```cpp
void handleNormalState() {
  CableStatus currentStatus = tester.runTest();

  static CableStatus debouncedStatus;
  static int stableCount = 0;

  // ⚠️ 防抖逻辑 1：连续 3 次稳定才保存
  if (CableTester::isStatusEqual(currentStatus, debouncedStatus)) {
    stableCount++;
  } else {
    stableCount = 0;
    debouncedStatus = currentStatus;
  }

  if (stableCount == STABLE_READING_COUNT) {
    // ... 渲染并保存历史
  }

  // ⚠️ 防抖逻辑 2：首次运行立即渲染（绕过防抖！）
  if (isFirstRun) {
    // ... 重复的渲染代码，但不保存历史！
    isFirstRun = false;
  }
}
```

### 风险

- `isFirstRun` 时渲染的结果**不会被保存到历史记录**
- 如果插入网线后立即切换菜单，该测试结果永久丢失
- 屏幕可能在 `stableCount` 和 `isFirstRun` 之间闪烁

### 改进方案

统一两套逻辑，用单一的"是否需要更新"标志：

```cpp
void handleNormalState() {
  CableStatus currentStatus = tester.runTest();

  // 静态防抖状态
  static CableStatus debouncedStatus;
  static int stableCount = 0;

  // 标准防抖计数
  if (CableTester::isStatusEqual(currentStatus, debouncedStatus)) {
    stableCount++;
  } else {
    stableCount = 0;
    debouncedStatus = currentStatus;
  }

  // ✅ 统一判定：需要更新的条件
  bool shouldUpdate = isFirstRun || (stableCount == STABLE_READING_COUNT);

  if (shouldUpdate) {
    if (!CableTester::isNoCable(currentStatus)) {
      // 渲染结果
      display.renderResult(currentStatus, appConfig.useFeet, appConfig.isCalibrated);

      // ✅ 统一保存逻辑：所有有效结果都保存
      if (CableTester::hasActualLength(currentStatus) || currentStatus.hasFault) {
        if (!CableTester::isStatusEqual(currentStatus, lastStatus)) {
          lastStatus = currentStatus;
          appConfig.addHistory(currentStatus);
        }
      }
    } else {
      // 没插线时显示 Ready
      display.renderReady();
    }

    // ✅ 统一清理 isFirstRun
    isFirstRun = false;
  }
}
```

### 验证步骤

1. 开机后立即插入已知长度的网线 → 应在屏幕稳定后自动保存
2. 快速切换到历史菜单 → 最新测试结果应该被记录
3. 反复拔插网线 → 屏幕不应闪烁，只记录稳定状态

---

## 问题 P2: 注释风格不统一

### 现状问题

**文件**: `src/CableTester.cpp`, 第 48 行

```cpp
digitalWrite(rxPin, LOW); // 刚才就是漏了这句，导致根本没放电！
```

**风格问题**:
- 中英混合，且带有个人调试语气（"刚才就是漏了"）
- 不符合专业代码规范

### 改进方案

```cpp
// 完全放电，确保电容从 0V 开始充电（极其关键）
digitalWrite(rxPin, LOW);
delayMicroseconds(5000);  // 确保完全放电到 0V，时序严格固定
```

或保留更深度的物理注释：

```cpp
// 彻底放电至 0V：防止残余电荷导致下一次充电曲线异常
// 这是 PVC 介质电吸收误差的根源，务必严格控制 discharge 时间
digitalWrite(rxPin, LOW);
delayMicroseconds(5000);
```

---

## 问题 P3: 菜单项硬编码魔术数字

### 现状问题

**文件**: `src/main.cpp`, 第 140 行

```cpp
menuIndex = (menuIndex + 1) % 6;  // ⚠️ 为什么是 6？
```

### 风险

- 如果后续添加新菜单项（如语言切换），容易遗漏更新这个数字
- 无法快速查看总共有多少个菜单项

### 改进方案

在 `main.cpp` 顶部定义 enum：

```cpp
// === 设置菜单项定义 ===
enum SettingsMenuItem {
    MENU_VIEW_HISTORY = 0,    // 查看历史记录
    MENU_CLEAR_HISTORY = 1,   // 清空历史记录
    MENU_TOGGLE_UNIT = 2,     // 单位切换 (ft ↔ m)
    MENU_TOGGLE_SOUND = 3,    // 声音开关
    MENU_CALIBRATE = 4,       // 校准向导
    MENU_EXIT = 5,            // 退出设置
    MENU_COUNT                // 自动计算菜单项总数
};

AppState appState = STATE_NORMAL;
int menuIndex = 0;
```

然后在循环处使用：

```cpp
} else if (appState == STATE_SETTINGS) {
    if (singleClick) {
      menuIndex = (menuIndex + 1) % MENU_COUNT;  // ✅ 清晰且可维护
      display.renderSettings(menuIndex, appConfig.useFeet, appConfig.soundOn);
    } else if (doubleClick) {
      switch (menuIndex) {  // ✅ 配合 enum 使用，可读性更好
        case MENU_VIEW_HISTORY:
          if (appConfig.historyCount > 0) {
            appState = STATE_HISTORY_VIEW;
            appConfig.historyIndex = 0;
            display.renderHistory(appConfig.historyLogs[appConfig.historyIndex], 
                                  appConfig.useFeet, appConfig.historyIndex, 
                                  appConfig.historyCount);
          } else {
            display.renderMessage("No History!");
            delay(1000);
            display.renderSettings(menuIndex, appConfig.useFeet, appConfig.soundOn);
          }
          break;
          
        case MENU_CLEAR_HISTORY:
          appConfig.clearHistory();
          printf("History Cleared!\n");
          display.renderMessage("History Cleared!");
          delay(1000);
          display.renderSettings(menuIndex, appConfig.useFeet, appConfig.soundOn);
          break;
          
        // ... 其他菜单项处理
      }
    }
}
```

### 优点

- 新增菜单项时，只需在 enum 中添加一行
- `MENU_COUNT` 自动扩展，无需手工维护硬编码数字
- 代码意图更清晰，易于理解每个菜单项的含义

---

## 问题 P4: 超时异常处理

### 现状问题

**文件**: `src/CableTester.cpp`, 第 69~86 行

当轮询超时时，代码只有 `break`，没有明确区分原因：

```cpp
while (((*(volatile uint32_t *)(GPIO_IN_REG)) & pin_mask) == 0) {
    if (ESP.getCycleCount() - start > max_cycles)
        break;  // ⚠️ 为什么超时？是断路还是故障？
}
```

### 改进方案（可选，当前实现已有超时保护）

```cpp
uint32_t start = ESP.getCycleCount();
uint32_t max_cycles = MAX_POLL_CYCLES;
bool timedOut = false;

// 轮询等待直到引脚变为高电平
uint32_t pin_mask = (1 << (rxPin & 31));
if (rxPin < 32) {
    while (((*(volatile uint32_t *)(GPIO_IN_REG)) & pin_mask) == 0) {
        if (ESP.getCycleCount() - start > max_cycles) {
            timedOut = true;
            break;
        }
    }
} else {
    while (((*(volatile uint32_t *)(GPIO_IN1_REG)) & pin_mask) == 0) {
        if (ESP.getCycleCount() - start > max_cycles) {
            timedOut = true;
            break;
        }
    }
}

uint32_t end = ESP.getCycleCount();
uint32_t cycles = end - start;

// ✅ 添加调试日志
if (timedOut) {
    printf("[TDR] Pin %u measurement timed out at %u cycles (expected ~24M)\n", 
           rxPin, cycles);
}

return cycles;
```

这样在需要调试时，串口会输出超时原因。

---

## 综合修改检查清单

| 问题 | 文件 | 行号 | 改动范围 | 测试场景 |
|-----|------|------|---------|---------|
| P0: 校准参数检验 | `main.cpp` | 194~223 | ~30 行 | 使用非标准长度校准 |
| P1: 防抖逻辑统一 | `main.cpp` | 59~99 | ~25 行（删除冗余代码） | 快速拔插网线 |
| P2: 注释统一 | `CableTester.cpp` | 48 | 1 行 | 代码审查 |
| P3: 菜单 enum 化 | `main.cpp` | 20~35 + 140+ | ~35 行新增+修改 | 菜单切换测试 |
| P4: 超时日志 | `CableTester.cpp` | 69~86 | ~10 行（可选） | 串口日志输出 |

---

## 修改优先级建议

1. **立即应用 P0** ✅ - 防止用户误操作导致产品失效
2. **同步应用 P1** ✅ - 修复潜在的数据丢失问题
3. **应用 P2+P3** 🔄 - 提升代码质量（可与 P0/P1 同期发布）
4. **可选应用 P4** 📝 - 仅用于调试和后续迭代

---

**修改预计工作量**: 2~3 小时
**预期效果**: 功能完整性不变，代码健壮性显著提升
